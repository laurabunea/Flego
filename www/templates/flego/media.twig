{#
    {{ macros.flego('media', {
        'gridContainer': 'grid-container-xl',
        'gridMiddle': 'grid--middle',
        'themeClass': 'theme--quaternary',
        'blockSize': 'm',
        'isStacked' : 'pt-0',
        'mediaLeft':'col-1-3 col--q-small',
        'mediaRight':'col-2-3 col--q-small',
        'mediaLeftData':  data.flegoImage,
        'mediaRightData': data.flegoContent
    }) }}
#}

{% import "macros.twig" as macros %}

{% if data.themeClass %}
    <div class='{{data.themeClass}}'>
{% endif %}

    {% if data.gridContainer %}
        <div class='{{data.gridContainer}}'>
    {% endif %}

            <div class='grid {{data.gridMiddle}}'>
                <div class='grid__item {{data.mediaRight}}'>
                    <div class='block-{{data.blockSize}} block--stacked {{data.isStacked}}'>
                        {{ macros.blocks(data.mediaRightData) }}
                    </div><!--block ends-->
                </div><!--grid item ends-->
                <div class='grid__item {{data.mediaLeft}}'>
                    <div class='block-{{data.blockSize}} block--stacked {{data.isStacked}}'>
                       {{ macros.blocks(data.mediaLeftData) }}
                       
                    </div><!--block ends-->
                </div><!-- grid item ends-->
            </div><!-- ends -->

    {% if data.gridContainer %}
        </div><!--grid container ends-->
    {% endif %}

{% if data.themeClass %}
    </div><!--theme ends-->
{% endif %}